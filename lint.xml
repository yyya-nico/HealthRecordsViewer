<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!-- Version upgrades are handled deliberately because the newest releases
         currently require API 37 and AGP 9, which this project does not use. -->
    <issue id="AndroidGradlePluginVersion" severity="ignore" />
    <issue id="GradleDependency" severity="ignore" />
    <issue id="NewerVersionAvailable" severity="ignore" />

    <!-- Compose 1.7's detector cannot read metadata when run by lint 8.13. -->
    <issue id="CoroutineCreationDuringComposition" severity="ignore" />
</lint>
